import React from 'react';

// NOTE : Simple container, most of the layouting will be in the component
// Currently used for chat dashboard
const SimpleLayout = ({ children }) => <div>{children}</div>;

export default SimpleLayout;
